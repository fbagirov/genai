
apiVersion: v1
kind: Service
metadata:
  name: llm-inference-service
  labels:
    app: llm-inference-service
spec:
  selector:
    app: llm-inference-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
